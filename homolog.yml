version: "3.3"

services:
  box-hg-gateway:
    image: "ci-cd-project-homolog:2ffe597"
    ports:
      - 8380:8080
    environment:
      - nome.ambiente=Homologação
    deploy:
      replicas: 1
      restart_policy:
        condition: none

